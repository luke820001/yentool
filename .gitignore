.env
data/
__pycache__/
*.pyc

# PyInstaller build artifacts
build/
dist/
*.spec
# ...except the single packaging entry point (report F26: two specs disagreed).
!TaiwanScanner.spec

# Legacy pre-SQLite dump kept for reconciliation only, not version control.
archive/legacy/*.xlsx
